Simple application to manage the upload of images and save them in a MongoDB

INITIAL SETUP
Project initialized with:
    * mkdir image-uploader
    * cd image-uploader
    * npm init
    * npm install express

EXECUTION
npm install
npm run start
http://localhost:3000

To access the swagger documentation:
http://localhost:3000/api/swagger-ui.html/
